namespace hzmj
{
    public class Program
    {
        private static void Main()
        {
            // 湖南麻将红中麻将配置
            //HashesGen.Generate(4, CardType.Num, "hz");

            // 溪水麻将配置
            //HashesGen.Generate(4, CardType.Num, "num");
            //HashesGen.Generate(4, CardType.Feng, "feng");
        }
    }
}